version: '3.8'

services:
  postgrest_nlw:
    container_name: postgresql_nlw
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_nlw